@model PagedList.Core.IPagedList<WebApplication1.ViewModels.LevelsListViewModel>

<table class="table table-hover">
    <thead class="thead-light">
        <tr>
            <th>Id</th>
            <th>Level Name</th>
            <th>Active</th>
            <th>Amount Category</th>
            <th>Amount Product</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Any())
        {
            @foreach (var level in Model)
            {
                <tr>
                    <td>
                        #@level.LevelCode
                    </td>
                    <td>
                        <div class="d-flex align-items-center">
                            <strong>@level.LevelName</strong>
                        </div>
                    </td>
                    <td>
                        @if (level.LevelActive == true)
                        {
                            <div class="d-flex align-items-center">
                                <div class="badge badge-success badge-dot m-r-10"></div>
                                <div>Active</div>
                            </div>
                        }
                        else
                        {
                            <div class="d-flex align-items-center">
                                <div class="badge badge-danger badge-dot m-r-10"></div>
                                <div>Non-Active</div>
                            </div>
                        }
                    </td>
                    <td>
                        @level.AmountCategory
                    </td>
                    <td>
                        @level.AmountProduct
                    </td>
                    <td class="text-right">
                        <a class="btn btn-icon btn-hover btn-sm btn-rounded" onclick="return confirm('Are you sure?')" asp-action="DeleteLevel" asp-area="Admin" asp-controller="AdminWarehouse" asp-route-id="@level.LevelCode">
                            <i class="anticon anticon-delete"></i>
                        </a>
                        <a class="btn btn-icon btn-hover btn-sm btn-rounded pull-right" asp-controller="AdminWarehouse" asp-area="Admin" asp-action="UpdateLevel" asp-route-id="@level.LevelCode">
                            <i class="anticon anticon-edit"></i>
                        </a>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>