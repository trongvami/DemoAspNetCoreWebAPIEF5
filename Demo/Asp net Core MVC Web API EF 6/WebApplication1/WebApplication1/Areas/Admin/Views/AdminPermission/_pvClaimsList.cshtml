@using WebApplication1.ViewModels;
@model WebApplication1.ViewModels.RoleClaimsViewModel

<table class="table table-hover">
    <thead class="thead-light">
        <tr>
            <th></th>
            <th>STT</th>
            <th>Claim Name</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.listClaims.Any())
        {
            int check = 1;
            @foreach (var claim in Model.listClaims)
            {
                <tr>
                    <td>
                        @*<input type="hidden" name="listClaims[@(check - 1)].ClaimType" value="@claim.ClaimType">
                        <div class="checkbox">
                            <input id="@claim.ClaimType" type="checkbox" value="true" name="listClaims[@(check - 1)].IsSelected" @(claim.IsSelected ? "checked" : "")>
                            <label for="@claim.ClaimType" class="m-b-0"></label>
                        </div>*@
                        <div class="checkbox">
                            <input id="@claim.ClaimType" type="checkbox" value="@claim.ClaimType" name="IsSelected" @(claim.IsSelected ? "checked" : "")>
                            <label for="@claim.ClaimType" class="m-b-0"></label>
                        </div>
                    </td>
                    <td>
                        @check
                    </td>
                    <td>
                        <div class="d-flex align-items-center">
                            <h6 class="m-b-0 m-l-10">@claim.ClaimType</h6>
                        </div>
                    </td>
                </tr>
                check = check + 1;
            }
        }
    </tbody>
</table>